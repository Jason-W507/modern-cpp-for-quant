# Tickets: Modern C++ 量化教程

基于已批准设计规格，构建一部面向 Python 量化从业者的 C++20 教程、配套代码与贯穿式事件驱动回测引擎。

来源规格：`docs/superpowers/specs/2026-07-11-modern-cpp-quant-book-design.md`。

按 frontier 工作：只有当前一项完成并验证后，下一项才解除阻塞。

## 建立可发布的书籍骨架

**What to build:** 创建基于 ElegantBook 的模块化书稿、示例工程入口与 MiKTeX 构建路径，并用完整第 1 章证明从源码到 PDF 的发布链路可用。

**Blocked by:** None — can start immediately.

- [x] 原模板目录保持不变，项目包含所需模板资产和许可说明
- [x] 模块化 `main.tex`、章节与附录目录能够通过 MiKTeX/XeLaTeX 构建
- [x] 第 1 章完整覆盖 Python 到 C++ 的编译、链接、UB 与零成本抽象心智切换
- [x] 至少一个 C++20 示例可由项目构建系统编译并运行

## 完成类型与 Python 迁移基础

**What to build:** 通过第 2 章和可运行例子，使 Python 开发者掌握 C++ 类型系统、值类别、引用与 `const` 的工作语义。

**Blocked by:** 建立可发布的书籍骨架。

- [x] 第 2 章包含 Python 对照、量化案例、常见错误、面试检查点和练习
- [x] 配套例子通过 C++20 编译检查并能演示值语义与引用边界
- [x] PDF 中代码清单与真实源码一致且可读

## 完成所有权与现代数据管道

**What to build:** 通过第 3–4 章和真实项目类型，让读者使用 RAII、智能指针、STL 算法与 ranges 处理行情数据。

**Blocked by:** 完成类型与 Python 迁移基础。

- [x] 第 3–4 章完整且包含章末练习
- [x] 行情、订单与成交采用清晰值语义和显式所有权
- [x] 数据管道例子可构建、运行并产生已知输出

## 完成量化组件接口与泛型约束

**What to build:** 通过第 5–6 章形成可替换的策略、事件与撮合接口，并用 concepts 表达编译期契约。

**Blocked by:** 完成所有权与现代数据管道。

- [x] 第 5–6 章从公开接口解释类设计、值语义、模板和 concepts
- [x] 策略和撮合组件通过用户确认的公开 seam 完成 TDD
- [x] 测试验证可观察行为，不耦合私有实现

## 完成可靠性与工程工具链

**What to build:** 通过第 7–8 章交付可诊断的 CSV 输入、清晰错误边界、CMake/CTest 和最小端到端回测。

**Blocked by:** 完成量化组件接口与泛型约束。

- [x] 第 7–8 章完整区分程序员错误、外部错误与热路径控制流
- [x] CSV 错误和最小端到端回测由公开接口测试覆盖
- [x] CMake 配置、单元测试和端到端测试可重复运行

## 完成性能工程主线

**What to build:** 通过第 9–10 章和可重复基准，让读者用数据布局、缓存、分配、benchmark 与 profiling 证据指导优化。

**Blocked by:** 完成可靠性与工程工具链。

- [x] 第 9–10 章不包含无测量依据的性能断言
- [x] 至少一组基准比较两种数据布局或分配策略
- [x] 文档记录环境、方法、噪声来源和结论限制

## 完成并发、数值与 Python 边界

**What to build:** 通过第 11–12 章交付并发正确性、数值精度和 Python/C++ 互操作边界的工作知识与示例。

**Blocked by:** 完成性能工程主线。

- [x] 第 11–12 章完整覆盖停止协议、同步、浮点误差和互操作所有权
- [x] 并发示例可重复结束且无数据竞争设计
- [x] pybind11 以接口设计和最小示例呈现，不成为大型依赖主线

## 完成求职闭环与出版验收

**What to build:** 组装贯穿项目，完成求职章节和四个附录，并对代码、书稿与最终 PDF 做全量验收。

**Blocked by:** 完成并发、数值与 Python 边界。

- [x] 第 13–14 章及工具链、术语、答案、30 天路线四个附录完整
- [x] 回测项目可构建、测试并运行最小样例，输出已知绩效摘要
- [x] 所有关键示例通过 C++20 编译，全量测试通过
- [x] MiKTeX/XeLaTeX 从干净构建目录生成最终 PDF
- [x] 编译日志无未定义引用、缺失字形或致命错误，并抽查版面警告
- [ ] code-review 的 Standards 与 Spec 两轴无未解决高优先级问题
