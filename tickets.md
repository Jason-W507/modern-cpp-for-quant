# Tickets: Modern C++ 量化教程第二版扩写

把当前 62 页初稿扩写为适合 C++ 零基础 Python 量化读者自学的 18 章、约 145–155 页教程。源规格见 `.scratch/second-edition-expansion/PRD.md`。

Work the **frontier**: any ticket whose blockers are all done.

## 建立第二版章节验收骨架

**What to build:** 建立一套能判断章节是否真正可自学的共同标准，并为后续 18 章提供可持续验证的写作、代码和项目快照约定。

**Blocked by:** None — can start immediately.

- [x] 初学语法覆盖矩阵记录每个概念的首次教学、立即练习和后续复用位置。
- [x] 章节验收清单覆盖先修、七步教学闭环、Python 边界、失败实验、输出任务和答案反馈。
- [x] 完整代码清单、故意失败示例和项目阶段快照各有明确且可自动检查的约定。
- [x] 18 章五篇职责与约 145–155 页预算能够被后续验收使用，而不靠空白占位章节制造进度。
- [x] 当前 C++ 测试与 LaTeX 构建在引入验收骨架后仍保持绿色。

## 标杆章：类型、表达式与控制流

**What to build:** 让只会 Python 的读者从零掌握 C++ 初始化、基本类型、字面量、运算符、作用域、分支与循环，并完成一个可运行的行情筛选任务。

**Blocked by:** 建立第二版章节验收骨架.

- [x] 每个核心小节满足七步教学闭环，不再以单段概述代替语法教学。
- [x] 完整示例和练习答案参与 C++20 构建，错误示例说明预期诊断类别。
- [x] Python 名称/对象与 C++ 对象/初始化的差异通过可观察结果解释。
- [x] 读者输出任务能独立读取固定行情数据、分支筛选并循环汇总结果。
- [x] 章节通过章节学习单元验收并校准合理页数。

## 标杆章：对象生命周期与 RAII

**What to build:** 让完成基础语法的读者从对象生命周期出发理解值类别、复制移动、RAII、裸指针识读和智能指针选择，并能诊断典型悬空与双重释放风险。

**Blocked by:** 标杆章：类型、表达式与控制流.

- [x] 生命周期、引用、指针、复制移动和资源释放由一组递进的可运行实验串联。
- [x] `new`/`delete` 与 C 数组作为底层识读内容出现，现代值类型和 RAII 保持默认路径。
- [x] 至少一个故意失败实验可由 sanitizer 给出预期类别的证据。
- [x] Python 垃圾回收、上下文管理器与 C++ 确定性析构的边界解释清楚。
- [x] 本章与基础标杆章共同锁定后续章节的教学尺度。

## 第 1 章：首次构建与诊断

**What to build:** 让零基础读者在 Linux/WSL 与 GCC 主线上独立创建、编译、链接、运行第一个程序，并能区分编译、链接与运行期错误。

**Blocked by:** 标杆章：对象生命周期与 RAII.

- [x] 从程序入口、头文件、命名空间、语句和标准输出开始，不假设读者认识 C++ 标点与结构。
- [x] 单文件命令、最小 CMake 目标和干净构建均可复现。
- [x] 至少三个故障实验分别产生编译、链接和运行期问题。
- [x] Windows 平台差异以简短提示给出，不复制整套主线步骤。
- [x] 读者能从空目录完成本章输出任务并解释构建流水线。

## 第 3 章：函数与多文件组织

**What to build:** 让读者把单文件程序拆成函数与多个翻译单元，理解作用域、声明、定义、参数、返回、重载、引用和 `const` 的基本语义。

**Blocked by:** 标杆章：类型、表达式与控制流; 第 1 章：首次构建与诊断.

- [x] 函数声明、定义、调用和返回通过逐步重构展示。
- [x] 按值、只读引用和可变引用通过可观察的复制与修改行为比较。
- [x] 头文件、实现文件、include guard 与命名空间组成可构建的多文件程序。
- [x] 重复定义、缺失定义和悬空引用各有诊断练习。
- [x] 读者完成一个多文件行情计算工具并通过章节验收。

## 第 4 章：标准数据工具与 CSV

**What to build:** 让读者使用 `string`、`vector`、数组、范围循环、迭代器、算法和 lambda 读取并处理 CSV 行情数据。

**Blocked by:** 第 3 章：函数与多文件组织.

- [ ] 容器大小、容量、索引、迭代器与失效规则由可运行实验解释。
- [ ] 手写循环与标准算法在清晰度、前置条件和结果上进行对照，而不声称未经测量的性能优势。
- [ ] CSV 正常输入、空字段、非法数值和列数错误都有可观察行为。
- [ ] 至少一个 lambda 示例从普通函数逐步改写而来。
- [ ] 章节产出可复用的行情读取与统计函数。

## 第 5 章：类与行情分析器

**What to build:** 让读者使用 `struct`、`class`、作用域枚举、构造函数和成员函数完成一个多文件行情分析命令行程序，并形成第一个可独立构建的项目阶段快照。

**Blocked by:** 第 4 章：标准数据工具与 CSV.

- [ ] 类语法从公开数据记录逐步演化到维护不变量的类型。
- [ ] 构造后有效、访问控制和成员函数通过非法行情输入进行验证。
- [ ] 行情分析器从干净目录构建并输出约定的收益与摘要结果。
- [ ] 基础篇练习均有可运行答案和关键步骤说明。
- [ ] 阶段快照不依赖后续回测项目即可配置、构建和运行。

## 第 7 章：STL 与 ranges

**What to build:** 让读者在已掌握基础容器和算法后，使用迭代器、算法组合与 ranges 构造生命周期清楚的量化数据管道。

**Blocked by:** 标杆章：对象生命周期与 RAII; 第 5 章：类与行情分析器.

- [ ] 容器选择以访问模式、失效规则和所有权为依据，而不是容器清单。
- [ ] ranges 视图的惰性、借用与悬空风险由可运行的正确/错误对照说明。
- [ ] 关联容器、排序、查找、转换和归约均回到行情分析任务。
- [ ] 浮点归约顺序的限制与后续数值章节边界一致。
- [ ] 改造后的项目阶段快照保持行为测试绿色。

## 第 8 章：接口与值语义

**What to build:** 让读者把行情分析代码重构为维护明确不变量的事件、订单、成交和组合组件，并能选择组合、运行时多态或值语义。

**Blocked by:** 第 7 章：STL 与 ranges.

- [ ] 组件从具体用例和失败场景出发，不以 getter/setter 清单代替设计。
- [ ] 值语义、组合与动态多态各自有适用边界和成本说明。
- [ ] 订单、成交与组合更新的公开行为由测试保护。
- [ ] 非法数量、非有限价格和状态不一致得到清楚处理。
- [ ] 本章项目快照展示从脚本式分析器到领域组件的可运行演化。

## 第 9 章：模板与 concepts

**What to build:** 让读者从普通函数与接口出发掌握函数模板、类模板、实例化、约束、concepts 以及静态/动态多态的真实取舍。

**Blocked by:** 第 8 章：接口与值语义.

- [ ] 模板语法由一个非泛型实现逐步泛化，而不是直接展示复杂最终代码。
- [ ] 无约束错误与 concept 约束失败通过简化后的编译诊断比较。
- [ ] 编译期能力检查与运行时语义测试的边界明确。
- [ ] 模板代码体积、编译时间、ABI 和虚分派权衡有具体场景。
- [ ] 策略接口的静态或动态版本通过公开行为验证。

## 第 10 章：错误处理与可观测性

**What to build:** 让读者为正常缺席、外部失败与程序员错误选择不同机制，并能为 CSV、策略和组合故障保留上下文、异常安全与可观测证据。

**Blocked by:** 第 8 章：接口与值语义.

- [ ] 可选值、显式错误、异常和断言由同一组失败场景比较。
- [ ] 强异常保证通过状态更新前后可观察结果验证。
- [ ] 日志、指标和追踪的职责与热路径成本边界清楚。
- [ ] 错误只在合适边界记录一次，并保留行号、字段或领域上下文。
- [ ] 练习包含错误注入与行为断言，而不只要求概念复述。

## 第 11 章：CMake 与依赖

**What to build:** 让读者从基础篇项目出发理解 target-based CMake、可见性、传递依赖、构建配置和可复现的多目标工程。

**Blocked by:** 第 5 章：类与行情分析器.

- [ ] 可执行文件、库、测试和示例的目标关系由逐步重构建立。
- [ ] `PRIVATE`、`PUBLIC` 与 `INTERFACE` 通过真实传播行为解释。
- [ ] Debug、Sanitized 与 Release 配置各有明确用途和命令。
- [ ] 从干净目录配置和构建成功，不依赖未记录的本机状态。
- [ ] Windows 生成器差异不改变 Linux/WSL 主线心智模型。

## 第 12 章：测试与调试工具

**What to build:** 让读者使用行为测试、GDB、编译器警告、sanitizer 和静态分析定位真实故障，并识别 Release 下失效断言等测试基础设施问题。

**Blocked by:** 第 11 章：CMake 与依赖.

- [ ] 既有 CSV、策略、成交/组合和端到端回测 seam 作为行为测试先例。
- [ ] 至少一个红绿循环从失败测试走到最小修复。
- [ ] 越界、悬空、未定义行为和逻辑错误分别使用合适工具诊断。
- [ ] Release 测试仍执行真实检查，不依赖会被移除的标准断言。
- [ ] 读者任务要求从症状收集证据并定位根因，而非照抄命令。

## 第 13 章：布局、缓存与分配

**What to build:** 让读者通过可重复的小实验理解对象大小、对齐、局部性、AoS/SoA、分配和 false sharing，而不从复杂度或直觉直接得出性能结论。

**Blocked by:** 第 5 章：类与行情分析器; 第 12 章：测试与调试工具.

- [ ] 每个布局实验同时验证结果正确性和记录环境限制。
- [ ] 对象表示、连续访问与无关字段搬运通过可观察数据解释。
- [ ] arena 与预分配同时说明生命周期风险。
- [ ] false sharing 示例不把固定缓存行大小当作跨平台保证。
- [ ] 项目改造只有在实验提供证据后才进入阶段快照。

## 第 14 章：benchmark 与 profiling

**What to build:** 让读者把性能问题写成可反驳假设，建立基线、避免死代码删除、解释样本分布，并用 profiler 决定优化优先级。

**Blocked by:** 第 13 章：布局、缓存与分配.

- [ ] 微基准包含预热、重复样本、交替顺序、结果消费和正确性校验。
- [ ] 中位数、IQR、吞吐和尾延迟的含义与误用通过数据说明。
- [ ] profiler 结果连接到端到端预算和 Amdahl 定律。
- [ ] 书中不出现脱离硬件、编译器和工作负载的绝对性能结论。
- [ ] 读者能提交一份包含命令、环境、原始结果、结论和限制的实验报告。

## 第 15 章：并发与背压

**What to build:** 让读者从所有权和 happens-before 推理线程、互斥锁、原子、任务、停止和背压，并通过故障实验识别数据竞争与停止协议缺陷。

**Blocked by:** 标杆章：对象生命周期与 RAII; 第 12 章：测试与调试工具.

- [ ] 顺序基线、线程版本和任务版本保持可比较的正确结果。
- [ ] 互斥锁保护复合不变量，原子不被描述为多个字段的一致快照。
- [ ] 启动、停止、异常传播和缓冲满策略均有明确协议。
- [ ] 故意竞态通过 ThreadSanitizer 或等价证据说明，不使用 sleep 掩盖问题。
- [ ] 练习要求解释正确性边界和测量开销，而非追求无锁炫技。

## 第 16 章：数值与 Python 互操作

**What to build:** 让 Python 量化读者理解浮点表示、稳定归约、容差测试和 pybind11 的批量、dtype、连续性、GIL 与所有权边界。

**Blocked by:** 第 5 章：类与行情分析器; 第 14 章：benchmark 与 profiling.

- [ ] 浮点失败例子、稳定算法和容差来源均有可运行证据。
- [ ] 金额、收益和方差示例明确表示与舍入口径。
- [ ] Python/C++ 边界比较逐元素调用、批量复制和零拷贝视图的语义与成本。
- [ ] pybind11 可选模块与无依赖边界实现保持同一核心行为。
- [ ] 返回临时缓冲视图、错误 dtype 和 GIL 使用各有诊断案例。

## 第 17 章：演化式回测引擎

**What to build:** 将基础篇行情分析器和各章阶段快照汇合为可配置、可测试、可测量的事件驱动回测引擎，并清楚区分教学项目与生产平台。

**Blocked by:** 第 9 章：模板与 concepts; 第 10 章：错误处理与可观测性; 第 12 章：测试与调试工具; 第 14 章：benchmark 与 profiling; 第 15 章：并发与背压; 第 16 章：数值与 Python 互操作.

- [ ] CSV、策略、订单、成交、组合和统计的数据流由早期项目阶段自然演化而来。
- [ ] 既有四个公开行为 seam 和端到端手算账本全部通过。
- [ ] 章节说明费用、滑点、多资产、缺失价格与订单生命周期的明确边界。
- [ ] 最终项目从干净目录构建、测试、运行并输出可解释绩效摘要。
- [ ] 关键阶段快照分别可构建，且最终实现不使早期章节失效。

## 第 18 章：求职与入职

**What to build:** 让读者把逐章产生的代码、测试、调试和性能证据组织成量化 C++ 面试回答、作品集材料和入职前三个月计划。

**Blocked by:** 第 17 章：演化式回测引擎.

- [ ] 语言面试回答使用定义、适用场景、代价和失败例子的共同结构。
- [ ] 代码题、性能题和系统设计题均引用本书已经实践的证据。
- [ ] 项目描述包含可复现命令、测试、数字、限制和一次失败修复。
- [ ] 行为面试素材与技术项目事实一致，不生成无法证明的成果。
- [ ] 入职路线覆盖代码库阅读、构建测试、可观测性、性能基线和首次安全改动。

## 扩写附录与分层答案

**What to build:** 为第二版提供能让零基础读者恢复学习的分层答案、更新后的术语表、工具链速查、代码定位和学习路线。

**Blocked by:** 第 18 章：求职与入职.

- [ ] 基础篇全部练习有可运行答案与关键步骤，且不只重复题目。
- [ ] 进阶核心题有完整实现，其余题有提示、验收标准和常见错误。
- [ ] 术语表与正文用词、Python 边界和项目概念一致。
- [ ] 工具链速查以 Linux/WSL 主线为准，并保留本书 MiKTeX 构建说明。
- [ ] 学习路线按章节输出任务和可验证证据组织。

## 全书集成与发布验收

**What to build:** 交付一份结构连贯、代码全部可验证、MiKTeX 可编译且版面可读的 18 章第二版 PDF，并确认它真正满足零基础自学边界。

**Blocked by:** 扩写附录与分层答案.

- [ ] 18 章与附录全部通过章节学习单元检查，语法覆盖矩阵没有未解释前置概念。
- [ ] 所有完整示例、项目快照、最终项目和既有测试从干净构建通过。
- [ ] MiKTeX/XeLaTeX 生成约 145–155 页最终 PDF，交叉引用、目录和代码清单正确。
- [ ] 构建日志没有未定义引用、缺失字形、致命错误或影响阅读的溢出。
- [ ] 渲染抽查覆盖章节首页、代码密集页、表格、附录与最终页，未发现裁切、重叠或不可读内容。
- [ ] 原 ElegantBook 模板目录保持不变，发布产物文件名稳定。
