\chapter{练习答案与思路}

本附录给出检查点而非唯一实现。编码题应以可编译代码、行为测试和复杂度说明为最终答案。

\section{第 1 章}
\subsection*{自测答案}
\begin{enumerate}
  \item \texttt{\#include} 在预处理阶段处理，不是以分号结束的 C++ 语句；它让当前翻译单元看见 \texttt{iostream} 中的声明。
  \item \texttt{main} 是程序入口名，空圆括号表示本版本不接收参数，花括号包围函数体；状态 0 表示成功，非零表示失败。
  \item \texttt{std::} 指定标准库命名空间，\texttt{<<} 把右侧文本送入输出流，双引号界定字符串字面量，\texttt{\textbackslash n} 换行，分号结束语句。
  \item 流水线是预处理、编译、链接和运行；\texttt{-c} 在目标文件处停止，因此能证明编译通过，却不能证明链接成功。
  \item 缺分号是编译失败；只有声明没有定义是链接失败；打印启动错误并返回 2 是运行期失败。修复分别恢复语法、加入定义、补齐启动条件。
  \item CMake 用 \texttt{-S} 指源目录、\texttt{-B} 指构建目录。删除旧构建树后从不存在的 \path{build/} 成功配置、构建和运行，才是干净构建证据。
  \item Windows 可执行文件带 \texttt{.exe}，路径分隔和编译器警告选项不同，多配置生成器还包含配置目录；预处理、编译、链接、运行的阶段模型不变。
\end{enumerate}

\subsection*{编码任务反馈}
\begin{enumerate}
  \item 完整文件见 \path{code/ch01/first_program.cpp} 与 \path{code/ch01/CMakeLists.txt}。直接 GCC 和干净 CMake 构建都应输出 \texttt{quant-cpp-ready} 并返回 0。
  \item 三个失败源依次是 \path{missing_semicolon.cpp}、\path{missing_definition.cpp} 与 \path{startup_failure.cpp}，均位于 \path{code/ch01/failures/}；验收类别分别是编译期缺分号、链接期缺定义、运行期消息加状态 2。
  \item \texttt{//} 到行末结束，\texttt{/* ... */} 可以跨行但不能嵌套；两种注释都不应改变输出。删除分号应使编译失败。恢复后重跑 \texttt{ctest -R ch01\_}，五个第 1 章行为测试应全部通过。
\end{enumerate}

\section{第 2 章}
\begin{enumerate}
  \item 初始化在对象创建时给出初始状态；赋值修改已存在对象。局部 \texttt{int x;} 没有可靠初值，\texttt{int x\{\};} 值初始化为零。字符字面量用单引号，字符串字面量用双引号；可运行演示见 \path{code/ch02/objects_and_literals.cpp}。完整筛选答案见 \path{code/ch02/market_filter.cpp}，给定五行输入应输出 \texttt{selected=2 rejected=3 buy\_notional=1035.75 average=517.875}。
  \item \texttt{auto} 仍在编译期推导出固定类型；当位宽或领域含义重要时显式写出类型。\texttt{5 / 2} 先做整数除法得到 2，\texttt{5.0 / 2} 得到 2.5；平均值分母可写成 \texttt{static\_cast<double>(selected)}。
  \item \lstinline|a && b| 在 \texttt{a} 为假时不求值 \texttt{b}。\texttt{switch} 漏写 \texttt{break} 会贯穿后续标签；\texttt{default} 接住未识别方向。已知迭代次数用 \texttt{for}，由状态决定终止用 \texttt{while}。
  \item 风险预算题的完整答案见 \path{code/ch02/exercises/risk_budget_solution.cpp}；3200 按每笔 750 扣减四次后剩 200。计数循环的独立答案见 \path{code/ch02/for_scope.cpp}。若循环体不扣减预算，条件永不改变，会形成无限循环。
  \item \path{code/ch02/failures/narrowing.cpp} 应在编译阶段得到“列表初始化拒绝窄化转换”类别。可把接收对象改为 \texttt{double}；若业务确实需要整数，先定义截断、四舍五入或拒绝策略，再显式转换。
\end{enumerate}

\section{第 3 章}
\subsection*{自测答案}
\begin{enumerate}
  \item 声明给出返回类型、名称和参数表并以分号结束；定义给出匹配接口与函数体；调用用实参创建参数对象并取得结果。定义的右花括号后不加函数声明式分号。
  \item 按值或引用参数在调用期间有效，局部对象在离开函数时销毁；返回值交回调用者。早返回在满足条件时立即结束本次调用，跳过后续语句。
  \item 按值允许修改独立参数而不影响调用者；\texttt{const T\&} 只读借用；\texttt{T\&} 可修改调用者；\texttt{const T*} 是可空的只读观察，检查不为 \texttt{nullptr} 后才能解引用。
  \item 重载靠参数数量或类型区分，并要求调用时存在唯一最佳候选。只改变返回类型无法总由实参确定选择，因此不能形成可调用的重载集合。
  \item 每个 \texttt{.cpp} 连同包含的声明形成独立翻译单元并编译为目标文件；链接器把目标文件与库组合为可执行文件。头文件自身不会自动提供实现。
  \item include guard 让同一头文件在一个翻译单元重复包含时只展开一次。项目头文件用双引号配合目标 include 路径；命名空间降低项目名称与其他库冲突的风险。
  \item 缺失定义与重复定义都在各翻译单元编译后由链接器报告；返回局部对象引用可由高警告级别在编译时指出，其后读取是未定义行为，不运行来验收结果。
\end{enumerate}

\subsection*{编码任务反馈}
\begin{enumerate}
  \item 完整多文件答案位于 \path{code/ch03/include/quant/ch03/market_math.hpp}、\path{code/ch03/src/market_math.cpp} 与 \path{code/ch03/src/main.cpp}；独立 \path{code/ch03/CMakeLists.txt} 应从空构建树得到 \texttt{notional=4010.00 quantity=40 vwap=100.25}。
  \item 完整可运行答案如下；输出应为 \texttt{rate=0.0015 basis\_points=15}，并证明调用者值不变：
\lstinputlisting{code/ch03/exercises/basis_points_solution.cpp}
  \item 三个故障源码位于 \path{code/ch03/failures/}。缺失与重复定义分别匹配链接器的 undefined 与 multiple-definition 类别；悬空引用匹配返回局部对象引用的编译诊断。恢复方式分别是补上唯一定义、删除额外定义、改为按值返回或证明被引用对象寿命足够长。
\end{enumerate}

\section{第 4 章}
\subsection*{自测答案}
\begin{enumerate}
  \item \texttt{string} 保存拥有的字符序列；\texttt{vector<double>} 保存动态数量的同类型连续元素；\texttt{array<string, 3>} 的长度 3 属于类型。三者都不能像 Python list 一样随意混放无关类型。
  \item \texttt{size} 是已存在元素数，\texttt{capacity} 是当前分配可容纳数。\texttt{reserve} 只提高容量下界，\texttt{resize} 改变大小并构造或销毁元素。
  \item 半开区间包含 \texttt{first} 而不包含 \texttt{last}；空区间可由 \texttt{first == last} 表示，因此尾后迭代器只用于比较，不能解引用。
  \item vector 重新分配会使全部旧位置失效；插入或删除还可能使操作点及其后位置失效。可预留足够容量，或修改后重新取得迭代器、引用和指针。
  \item lambda 的 \texttt{[]} 是捕获列表，\texttt{()} 是参数表，\texttt{\{\}} 是函数体；空捕获不读取周围局部对象。\texttt{[minimum]} 按值捕获阈值。
  \item 操作正好是查找、计数、复制、排序、转换或归约时，算法直接表达意图；多状态、早停或逐行诊断常由普通循环更清楚。两者都必须满足范围、输出空间和迭代器有效性前置条件。
  \item 打开失败属于文件边界；列数与数值错误属于带行号的数据边界。三者均返回 2，但诊断分别为 cannot open、expected columns 与 invalid number。
  \item 三个平行 vector 的同一索引共同表示一行，因此长度必须相等。第 5 章用一个行情记录类型把 symbol、price、quantity 组合成单个元素，消除同步修改三列的脆弱约束。
\end{enumerate}

\subsection*{编码任务反馈}
CSV 读取与统计函数的完整实现如下；它只接受本章定义的三列方言，任何失败都会清空平行列并保留行号：
\lstinputlisting{code/ch04/src/csv_stats.cpp}

完整答案如下；使用 \path{code/ch04/data/market_rows.csv} 与参数 \texttt{AAPL} 时，应输出 \texttt{symbol=AAPL rows=2 quantity=15 notional=1505.00}：
\lstinputlisting{code/ch04/exercises/symbol_summary_solution.cpp}

空代码、非法价格和错误列数的固定输入与预期错误分别位于 \path{code/ch04/data/} 和 \path{code/ch04/expected/}。故意失效迭代器只通过 ASan 验收 \texttt{heap-use-after-free} 类别，不读取未定义的数值结果。

\section{第 5 章}
\subsection*{自测答案}
\begin{enumerate}
  \item \texttt{struct} 默认公开，\texttt{class} 默认私有；两者都能声明访问标签、构造函数和成员函数，不是两套对象模型。选择应表达“透明记录”还是“维护不变量的抽象”。
  \item 作用域枚举把枚举项保留在类型作用域并阻止静默整数转换，因此写 \texttt{Trend::up}。分支直接 \texttt{return} 或 \texttt{throw} 时不需 \texttt{break}；仍要继续执行函数时，\texttt{break} 防止贯穿下一标签。
  \item 构造函数建立对象，没有单独返回值。成员初始化列表在进入函数体前构造成员；函数体随后检查字段组合，不满足时抛出且对象不交付。
  \item \texttt{price() const} 的尾随 \texttt{const} 约束当前对象不被成员函数修改；\texttt{const std::string\&} 约束调用者通过返回引用只读观察字符串，并要求借用不超过对象寿命。
  \item \texttt{this} 是当前对象的指针；\texttt{this->prices\_} 通过指针访问当前对象成员，\texttt{quote.price()} 用点号从已有对象调用成员。无歧义时成员函数内可省略 \texttt{this->}。
  \item \texttt{MarketQuote} 的空代码、非法价格和非正数量会破坏所有后续统计，适合在构造边界拒绝。\texttt{MarketSummary} 是已经计算完成、无需继续维护跨字段状态的透明结果，适合公开字段。
  \item 异常传播会按逆序销毁已完成构造的自动对象；未完成构造的目标对象不存在。CLI 有能力决定 stderr 与退出码，因此在那里记录一次，内层只补充字段和行号上下文。
  \item 读取 \texttt{quantity\_} 违反访问控制，在编译期得到 private/inaccessible 类别；负价格语法有效但违反领域不变量，在运行期抛出并转成状态 2。恢复分别是调用公开观察函数、修正输入或在构造边界拒绝。
\end{enumerate}

\subsection*{编码任务反馈}
行情对象和分析器的完整实现如下；\texttt{add} 拒绝代码不一致，\texttt{summary} 拒绝空序列：
\lstinputlisting{code/ch05/src/market.cpp}

CSV 读取层保留表头、列数、数字字段和构造异常的行号上下文：
\lstinputlisting{code/ch05/src/csv_reader.cpp}

价格区间题的完整答案如下，输出应为 \texttt{symbol=AAPL range=1.00 trend=up}：
\lstinputlisting{code/ch05/exercises/price_range_solution.cpp}

独立快照入口为 \path{code/ch05/CMakeLists.txt}。私有访问实验只验收编译器的 private/inaccessible 类别；非法 CSV 固定输入和精确 stderr 位于 \path{code/ch05/data/} 与 \path{code/ch05/expected/}。

\section{第 6 章}
\subsection*{自测答案}
\begin{enumerate}
  \item 构造成功后对象生命周期开始，析构完成后结束；存储中的旧比特不会让已析构对象重新存在。自动对象按逆构造顺序销毁。
  \item \texttt{std::move} 只把表达式转换为可选择移动操作的值类别。移动后的源对象仍可析构、可赋值，但除非类型契约另有保证，不能假定保留原值。
  \item 引用和裸指针通常只借用；\texttt{unique\_ptr} 是可转移的唯一所有者；\texttt{shared\_ptr} 让多个参与者共同决定寿命。直接值仍是最简单的默认所有权。
  \item C 数组退化为指针后不再携带长度。\texttt{new[]} 建立的数组必须由 \texttt{delete[]} 释放；与标量释放形式混用是未定义行为。
  \item RAII 让栈展开和所有提前离开路径都触发析构；Rule of Zero 把释放交给可靠成员，避免外层类型手写一组容易不一致的复制、移动和析构操作。
  \item \texttt{weak\_ptr} 不增加强计数，因此可打破共享环；\texttt{shared\_ptr} 只协调控制块寿命，不会自动同步被管理对象的字段。
  \item ASan 报告同时给出分配、释放和非法访问位置。按这三处重建生命周期图，再恢复单一明确的所有者和不越界的借用期。
\end{enumerate}

\subsection*{编码练习反馈}
\begin{enumerate}
  \item 复制/移动轨迹的完整答案及精确输出见 \path{code/ch06/lifetime_trace.cpp} 与 \path{code/ch06/expected/lifetime_trace.txt}。
  \item 引用、可空观察、C 数组和手动释放的可运行答案见 \path{code/ch06/borrowing_and_legacy.cpp}。
  \item RAII 文件答案见 \path{code/ch06/raii_file.cpp}：输出流析构后输入流读到两行，输入流析构后文件才删除。
  \item 所有权输出任务见 \path{code/ch06/ownership_choices.cpp}；答案必须与对应 expected 文件逐字一致。
  \item 故意失败源码位于目录 \path{code/ch06/failures/} 的 \path{use_after_free.cpp}。ASan 应非零退出并报告 \texttt{heap-use-after-free}，不验收未定义的输出值。
\end{enumerate}

\section{第 7 章}
\begin{enumerate}
  \item 成交时间线按到达顺序追加和扫描，\texttt{vector} 的连续拥有存储适合批处理；仓位按代码查询，\texttt{map} 把键查找写进接口。若改用其他容器，必须给出访问与失效依据。
  \item \texttt{positions[key]} 会在缺失时插入值初始化的整数，适合累计；\texttt{positions.at(key)} 只查询，缺失时抛出异常，能暴露错误代码。
  \item 排序会改变顺序，因此复制后排序价格；\texttt{find\_if} 必须观察原时间线，才能回答“首笔卖出”而不是“排序后第一笔卖出”。
  \item view 创建后才把 99 改成 110，最终仍输出 220，证明筛选与转换在迭代时求值。view 按值拥有 adaptor 状态，却借用 \texttt{prices} 的元素与存储。
  \item 返回 view 时局部 \texttt{vector} 已析构，view 内的引用悬空。可让源容器由调用者拥有、在原作用域消费，或在返回前复制到拥有元素的容器。
  \item \texttt{back\_inserter} 把赋值转换为 \texttt{push\_back}，使 \texttt{ranges::copy} 能向初始为空的 \texttt{vector} 追加结果，而不要求预先创建可写元素。
  \item 初值决定累加器类型。\texttt{0.0} 让名义金额按 \texttt{double} 累加；写 0 会让累加器成为 \texttt{int}，每一步都可能截断小数。
  \item 浮点加法不满足数学上的结合律，舍入发生在每一步。本章只承诺固定程序与输入下的测试结果，不承诺任意重排逐位相同；稳定求和与容差来源见第 16 章。
\end{enumerate}

核心编码任务的完整答案如下。它先把惰性筛选结果物化，再排序拥有元素的副本，最后用 \texttt{take(2)} 限制输出：

\lstinputlisting[caption={过滤、降序排序并输出前两笔 AAPL 成交}]{code/ch07/exercises/top_prices_solution.cpp}

项目答案位于 \path{code/ch07/src/pipeline.cpp}，独立构建入口为 \path{code/ch07/CMakeLists.txt}。非法生命周期实验只通过 ASan 的非零退出与栈访问类别，不验收未定义输出。

\section{第 8 章}
\begin{enumerate}
  \item 单独 build 目录加入 sanitizer flags；全部测试绿色且无 sanitizer 报告才通过。
  \item 端到端权益应偏离 10100，并以“最终权益”行为消息失败。
  \item 只调用 run 并检查结果，不读取 Portfolio 的内部 map。
  \item GCC/Clang Debug、sanitizer、Release smoke 各覆盖不同故障；固定硬件另跑性能。
\end{enumerate}

\section{第 9 章}
\begin{enumerate}
  \item 在多个独立进程中重复 20 次交替顺序采样，比较中位数与 IQR；不要删除异常值而不说明。
  \item 对象缩小可能提高扫描密度，实际收益依赖读取字段与缓存层次。
  \item 若分配样本占比很低，不引入池；可先 reserve 并重测。
  \item 每线程独立 cache-line-aware 累计，join 后单线程归约。
\end{enumerate}

\section{第 10 章}
\begin{enumerate}
  \item 保存每次耗时，排序取中位数和四分位；同时验证每次 checksum。
  \item 假设应可反驳，例如“解析占 CPU 样本超过 40\%”；profile 后接受或拒绝。
  \item 指标必须带负载，如每秒百万事件下 \(p_{99}<X\)，且错误/丢弃为零。
  \item 需要显著业务容量或成本收益、可维护封装、回归基准和回退路径。
\end{enumerate}

\section{第 11 章}
\begin{enumerate}
  \item 按资产分区输入，每线程只写自己的结果 map，join 后合并。
  \item 阻塞传播背压；丢最新损失新信息；覆盖最旧损失历史，均需指标和业务许可。
  \item 先停接收，再通知消费者排空/取消，唤醒阻塞，join，最后释放依赖。
  \item TSan 报告定位冲突访问；修复应建立所有权分片或明确同步，而非加随机 sleep。
\end{enumerate}

\section{第 12 章}
\begin{enumerate}
  \item 容差来自收益报告最小单位和算法误差，使用绝对 + 相对组合。
  \item Welford 同时更新均值与平方差，避免大数相减；与两遍算法用高精度参考比较。
  \item 规定二维/一维 shape、float64、C contiguous、借用期、异常映射和批量返回。
  \item 若时间已在 NumPy 原生核，重写外层无益；若 Python 逐元素循环主导，批量 C++ 值得原型。
\end{enumerate}

\section{第 13 章}
\begin{enumerate}
  \item 手算权益应再减手续费；测试先红，再让成交/组合一次性记录费用。
  \item 价格映射带时刻与币种，缺价返回不完整或错误，不能默认为零。
  \item 典型状态为 new、partially filled、filled、cancelled、rejected；终态不能再成交。
  \item 报告必须列环境、命令、数据、重复、统计、checksum 和结论适用范围。
\end{enumerate}

\section{第 14 章}
\begin{enumerate}
  \item 每项证据可链接代码、测试、基准或事故复盘；“了解”不是证据。
  \item 删除“高性能、生产级”等无测量词，保留边界、行为和限制。
  \item 复盘应区分理解、算法、编码、测试与沟通时间，选一个改进动作。
  \item 图中至少含输入、排序、队列、消费者、状态、存储、恢复和指标/告警。
\end{enumerate}
